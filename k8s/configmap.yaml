apiVersion: v1
kind: ConfigMap
metadata: { name: cbt-config, namespace: cbt }
data:
  APP_ENV: production
  APP_PORT: "8080"
  CORS_ALLOWED_ORIGINS: https://cbt.example.edu
  PROMETHEUS_ENABLED: "true"
  MAX_CONCURRENT_USERS: "100000"
